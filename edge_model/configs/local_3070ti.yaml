experiment_name: local_rbcm_fuse_demo
seed: 42
device: cuda

paths:
  project_root: .
  image_data_root: Image_data
  output_root: outputs/edge_detection

dataset:
  train_dataset: BIPED
  val_dataset: BSDS500
  train_split: train
  val_split: all
  val_fraction: 0.15
  input_size: 384
  random_crop: true
  horizontal_flip: true
  vertical_flip: false

loader:
  batch_size: 4
  num_workers: 2
  pin_memory: true

model:
  name: rbcm_fuse
  in_channels: 3
  feature_channels: 32
  rbcm_reduction: 2
  alpha_init: 0.1

loss:
  dice_weight: 1.0
  local_weight: 0.3
  gate_sparsity_weight: 0.0001

train:
  epochs: 20
  learning_rate: 0.0001
  weight_decay: 0.0001
  mixed_precision: true
  log_interval: 10
  eval_interval: 1
  save_visual_interval: 1
  max_visual_samples: 8
